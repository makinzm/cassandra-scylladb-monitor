name: Prometheus CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-prometheus:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate Prometheus scrape targets
        run: bash scripts/validate-prometheus.sh
